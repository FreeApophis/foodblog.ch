<h2><%= context_title @ingredient.name %></h2>

<% context_action t('.edit'), edit_ingredient_path(@ingredient), :write %>
<% context_action t('.delete'), delete_modal(@ingredient), :remove %>

<%= @ingredient.description %>
